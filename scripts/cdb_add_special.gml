// adds to, or concatenates to the special variable of a given character
// arg0 = character index
// arg1 = amount or string
ds_grid_add(global.__cdb,min(global.__cdbCount,max(0,argument0)),1,argument1);

